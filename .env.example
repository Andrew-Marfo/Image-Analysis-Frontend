# Base URL of the backend image-analysis API.
# Leave unset to use the built-in mock extraction service (default for local dev/demo).
# When the backend is ready, set this and wire the real HTTP client in src/api/client.ts.
VITE_API_BASE_URL=
